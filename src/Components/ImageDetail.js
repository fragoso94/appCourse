import React from 'react';
import {View, Button, Text, StyleSheet} from 'react-native';

const ImageDetail = ({titulo}) => {
    return(
      <View>
          <Text>{titulo}</Text>
      </View>
    );
}

export default ImageDetail;